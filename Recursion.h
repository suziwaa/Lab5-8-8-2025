#pragma once
#pragma once
#include <vector>
using namespace std;

// Finds lowest prime factor of num
int findLowestPrimeFactor(int num);

// Recursively gets prime factors of num
vector<int> getPrimeFactors(int num);
